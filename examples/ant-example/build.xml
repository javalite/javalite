<?xml version="1.0" encoding="UTF-8"?>
<project name="Simple ActiveJDBC Example" default="clean" basedir=".">


    <property name="classes" value="classes"/>

    <path id="run_classpath">
        <pathelement location="${classes}"/>
        <fileset dir="lib">
            <include name="*.jar"/>
        </fileset>
    </path>

    <path id="build_classpath">
        <path refid="run_classpath"/>
        <fileset dir="build_time_libs">
            <include name="*.jar"/>
        </fileset>
    </path>

    <target name="clean">
        <delete dir="${classes}" failonerror="false"/>
        <mkdir dir="${classes}"/>
    </target>

    <target name="compile" depends="clean">
        <javac srcdir="src" destdir="${classes}" debug="on" optimize="off" deprecation="off">
            <classpath refid="run_classpath"/>
        </javac>
    </target>

    <target name="instrument" depends="compile">
        <java classname="activejdbc.instrumentation.Main">
            <sysproperty key="outputDirectory" value="${classes}"/>
            <classpath refid="build_classpath"/>
        </java>
    </target>


    <target name="run" depends="instrument">
        <java classname="activejdbc.ant.example.Main">
            <classpath refid="run_classpath"/>
        </java>
    </target>

</project>
